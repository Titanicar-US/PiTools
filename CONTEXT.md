# PiTools active repository state

- Repository: [Titanicar-US/PiTools](https://github.com/Titanicar-US/PiTools); PRs [#1](https://github.com/Titanicar-US/PiTools/pull/1) through [#8](https://github.com/Titanicar-US/PiTools/pull/8) are merged. PR #8 carries the release/deployment documentation and bounded local acceptance hardening. Current remote `main` is `8cb7e3dcc56ae6b342451df8202a9cb51d653431`; PR #6 merged at `41183458794a3dafa8cbfcf94c76398fb44e7baf`, PR #7 merged at `3cac81979318109780047969a843f7dd67acfa9c`, PR #8 merged at `8cb7e3dcc56ae6b342451df8202a9cb51d653431`, and replacement release [v0.1.2](https://github.com/Titanicar-US/PiTools/releases/tag/v0.1.2) remains published at `3cac81979318109780047969a843f7dd67acfa9c`.
- The Rust control plane owns GitHub authentication, durable PostgreSQL jobs, reconciliation, readiness, comments, Check Run controls, deterministic feedback mutation, typed CI patch validation/application, validation, branch pushes, and bounded operator API/CLI inspection.
- The production router exposes the verified webhook receiver only with application state; the unconfigured utility router no longer advertises a placeholder webhook endpoint.
- The TypeScript Pi worker runs separately over the bounded `pitools.pi.requests` NATS request/reply subject. It receives no GitHub App credentials and defaults to diagnosis-only output.
- Local deployment uses PostgreSQL, NATS, the HTTP server, a Rust worker, and an optional long-lived Pi worker. Helm values intentionally require external secrets and immutable image digests.
- The GitHub App Manifest callback exchanges the one-time GitHub code and returns credentials with `no-store`; PiTools does not persist them.

## Deliberate fail-closed boundaries

- Human merge/approval remains outside PiTools.
- CI Pi output is a bounded diagnosis/proposal; only exact allowlisted unified patches that declare and receive explicit Check Run approval, pass credential-free bubblewrap validation from a `.git`-free snapshot, pass exact staged-path and fresh-head reads, and record an auditable push with `PiTools-Job`, `PiTools-Repair`, and `PiTools-Head` trailers may mutate a PR. Repository policy cannot disable this CI mutation gate.
- Approved feedback and CI repair pushes now re-read the exact remote branch head immediately before pushing, so branch movement after validation fails closed.
- Review-comment automation outcomes are replied to on the originating review comment before the thread is resolved; issue-comment outcomes are recorded in a bounded PR-level comment because no review thread exists. Deterministic rejection is persisted so the same suggestion is not retried indefinitely, while operational failures leave feedback open.
- Raw webhook bodies are retained for at most seven days and the durable worker now runs an hourly purge; operator event history exposes delivery metadata and payload hashes rather than raw bodies.
- Stack detection produces a deterministic order; declared stack base updates and branch rebases require explicit approval.
- Force-push is prohibited by default; a history rewrite is admitted only for an exact `bot_owned_branches` entry when `allow_bot_force_push` is true, and uses `--force-with-lease` against the fresh head.
- Fork-head repairs fail closed until a separately scoped head-repository push contract is added.

## Current validation evidence

- `make check` passed after the runtime watchlist, App-status, Check Run context, and completed-control cleanup fixes on 2026-08-11: Rust formatting, Clippy with warnings denied, the locked Rust test suite, 42 BDD scenarios (133 steps passed), 20 Pi worker tests, and the fail-closed Helm security render contract. The focused contracts cover paginated GitHub App identity/installations and mismatched-App rejection, repository/PR-bound Check Run controls, completed Check Run action removal, bounded Actions logs, check-run annotations, envelope redaction, extended provider/cloud/package/JWT/PEM redaction, oversize rejection, canonical path rejection, typed CI approval admission with a visible proposal fingerprint, credential-free validation sandbox construction, exact staged-path and staged-metadata admission, the Postgres active-watchlist query, protected operator routes, per-PR job/audit/event history, CLI command discovery, review-thread feedback replies, issue-comment outcome rendering, work-plan start/final-summary notifications, waiting-approval Skip/Cancel behavior, stack partitioning and delayed base updates, protected-branch review/count/staleness/code-owner gates, required-check app identity, installation lifecycle deactivation, and advisory readiness gates for conflicts, stale branches, failed or pending checks, missing approvals, unresolved automation feedback, and fully ready PRs. A disposable Postgres schema also exercised the cross-PR control rejection against the real repository and queue path.
- A fresh `KUBECONFIG=/dev/null make check` passed on 2026-08-12 after adding the bounded Docker failure contract and removing the non-standard `rg` dependency from the acceptance path: Rust formatting/Clippy/tests, the BDD suite, 21 Pi worker tests, Helm security rendering, shell syntax, the acceptance timeout contract under a stripped PATH, and the publish contract all passed. The focused shell contract simulates an unresponsive Docker CLI, and `PITOOLS_DOCKER_TIMEOUT_SECONDS=1 make acceptance` against the actual unresponsive local daemon exited with the expected Docker daemon/socket diagnostic and no lingering acceptance processes. A full Compose acceptance was not re-established at this head because the local Docker Desktop daemon/socket was unresponsive; no repository or cluster resources were left running by the diagnostic attempt.
- The generated Cargo trees in the active and adjacent handoff checkouts were removed on 2026-08-13 with Cargo cleanup; the workspace fell from approximately 27 GB of generated output to 288 MB. `make maintenance` now cleans the current checkout's Cargo and Pi worker outputs, and the weekly/manual maintenance workflow verifies those paths remain disposable.
- The root `LICENSE` now exactly matches the canonical Apache-2.0 text from `https://www.apache.org/licenses/LICENSE-2.0.txt`; Cargo package metadata also declares `Apache-2.0`. GitHub's license endpoint reports `Apache-2.0` for current `main` at `8cb7e3dcc56ae6b342451df8202a9cb51d653431`.
- The hosted `check` job now provisions the repository's pinned Postgres 17.6 Alpine image and sets a non-secret `DATABASE_URL`, so the queue integration contracts execute against an isolated schema instead of skipping in CI. The exact hosted run passed the coordinator notification lifecycle, active-watchlist query, queue hardening, approval fingerprint, cancellation fencing, and waiting-item Skip/Cancel contracts.
- Post-merge `main` at exact SHA `8cb7e3dcc56ae6b342451df8202a9cb51d653431` passed `check`, `check-full`, and `quality-gates` in workflow [31685867919](https://github.com/Titanicar-US/PiTools/actions/runs/31685867919), plus Rust, JavaScript/TypeScript, and Actions CodeQL analyses in workflow [31685867636](https://github.com/Titanicar-US/PiTools/actions/runs/31685867636).
- The canonical release helper is confirmation-gated, accepts only `vMAJOR.MINOR.PATCH`, targets `Titanicar-US/PiTools` `main`, and its fake-`gh` contract test passed. Release [v0.1.2](https://github.com/Titanicar-US/PiTools/releases/tag/v0.1.2) is pinned to `3cac81979318109780047969a843f7dd67acfa9c`; build-image workflow [31563225994](https://github.com/Titanicar-US/PiTools/actions/runs/31563225994) passed source validation, both architecture-specific image validations, and both image publications. Deployable manifests are `ghcr.io/titanicar-us/pitools@sha256:50c8ae460d0f873f35138dbc7a04d00f9bf8465f434aebd7169410997a397c3d` and `ghcr.io/titanicar-us/pitools-runner@sha256:35d01c8332d0ea62b0df5dd67d6c782a09b7c4b1dbe3c775b78fa8391e391fe4`.
- The image workflow now runs its publication job only for a `v` tag ref, rejects non-semantic tag names, and requires the tag to match both the Cargo and Pi worker package versions before package credentials or image pushes are used. The next unreleased package version is `0.1.3`; existing release [v0.1.2](https://github.com/Titanicar-US/PiTools/releases/tag/v0.1.2) remains the current immutable deployment release.
- Helm render contracts now require an explicit Pi-worker provider HTTPS allowlist, keep provider SDK execution disabled by default, require a separate provider Secret and explicit key mappings when enabled, and reject application/GitHub/control-plane credential boundaries.
- `/readyz` now fails closed when NATS is absent or its bounded flush probe fails, in addition to the PostgreSQL connectivity check; it still does not claim GitHub or model-provider readiness.
- The latest ephemeral local Compose acceptance passed against handoff head `d4f5de72dbb7ef1a546fb0487c6a6e6b92b7b93f` with non-production credentials using `rtk make acceptance`: the core image built, PostgreSQL and NATS became healthy, `/healthz` and `/readyz` returned success, unauthenticated operator access returned 401, a signed PR webhook returned 202, its duplicate returned 208, and watchlist/event persistence plus webhook metrics were observed. The unique Compose project and temporary credentials were removed after the run; the Check Run context guard remains covered by the focused disposable-Postgres contract.
- A fresh local Compose acceptance passed on 2026-08-11 against the then-current checkout using `rtk make acceptance`: the core image built, PostgreSQL and NATS became healthy, `/healthz` and `/readyz` returned success, unauthenticated operator access returned 401, a signed PR webhook returned 202, its duplicate returned 208, and watchlist/event persistence plus webhook metrics were observed. The unique Compose project and temporary credentials were removed after the run. The current acceptance script additionally bounds Docker/Compose commands so an unavailable daemon fails closed instead of hanging.
- Docker Compose 5.3.1 warns that local secret `uid`, `gid`, and `mode` settings are ignored; the local acceptance still passed because the generated source key remained readable to the test container, while production Helm security remains enforced separately.
- `make build` passed, and both local non-root images built successfully: core `sha256:d8c8713f14a2831b4039f546cb9fe5d4746a3db874f8aef00ad0ba1a35639ad6` and runner `sha256:b6519c75294ad64e009a217b5ecf9437b7c0f9b7cf78562bf008b35352c9b87c6`.
- Helm security rendering, local Compose validation with dummy non-production variables, the non-root core image build (`sha256:98a160382142b3ff3d9c79861ccc8d267ab85fb3c84132f17857fe3bf0c291`), and the corrected pinned-NATS request/reply smoke have passed. The core image reports `pitools 0.1.0`; the runner returns request-bound diagnosis-only JSON when no provider workflow is selected.
- The merged Pi worker now uses `@earendil-works/pi-coding-agent@0.84.1`; local `npm audit --omit=dev` reports zero vulnerabilities after the dependency update in [PR #2](https://github.com/Titanicar-US/PiTools/pull/2). Dependency Advisor's `standard` policy (336-hour minimum age) still recommends `0.82.1`, so `0.84.1` is recorded as an explicit security exception selected to clear the provider's current transitive advisories; the audit gate remains enforced.

## External release prerequisites

- The desktop checkout is an uncommitted scaffold without a usable local delivery `HEAD`; the authenticated Git Data fallback was used to publish the repository-owned source and follow-up PRs. Remote GitHub refs, workflow runs, and release records are the authoritative delivery evidence.
- Live delivery still requires GitHub App credentials and installation, dev01/Flux ownership, external secrets, database/NATS endpoints, and authenticated webhook/PR UAT. Merge and review authority remain user-owned.
- The default Pi worker is diagnosis-only and no provider-backed run was performed here. Provider-backed jobs use a per-request temporary Pi agent directory under `TMPDIR` unless `PITOOLS_PI_AGENT_DIR` is explicitly configured. Typed CI patch proposals and the approval-gated executor are implemented, but a live provider, repository allowlist, and fork-head write contract are intentionally not assumed.
- Read-only dev01 discovery on 2026-08-11 found `kubectl` and `flux` installed with the default context, but only the `flux-system` namespace; no `pitools` namespace, HelmRelease, or Kustomization was present, and no matching infrastructure repository was found under `/root` at the inspected depth. No dev01 state was mutated.
- Read-only inspection on 2026-08-13 confirmed the Flux source is healthy at `main@sha1:30603c01`, the dev01 Kustomizations are healthy, and the Flux source repository's application aggregate is `platform/targets/k8s-flux/apps/kustomization.yaml`. No PiTools entry or overlay is present, and a label-scoped read found no PiTools Deployment, Service, HTTPRoute, or NetworkPolicy; the Flux owner must wire the deployment path before a dev01 change can be prepared.
- Stack planning, approved PR base-branch updates, leased branch-history rebases, bounded check-run output plus Actions job-log/annotation evidence, and low-cardinality Prometheus metrics are implemented. Provider-backed repair, authenticated UAT, and full deployment evidence remain external prerequisites.
- Actions job-log evidence now resolves the distinct workflow job ID from an Actions check-run details URL before calling the job-log endpoint; malformed or non-Actions URLs fail closed, with regression coverage.
